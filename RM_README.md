# pm
# 📋 PM 需求單管理看板

> 版本：v66 ｜ 純前端單頁應用（Single HTML File）｜ 無需伺服器

---

## 目錄

- [功能介紹](#功能介紹)
- [操作說明](#操作說明)
- [Excel 匯入匯出格式](#excel-匯入匯出格式)
- [程式架構](#程式架構)
- [函數清單](#函數清單)
- [資料結構](#資料結構)
- [技術依賴](#技術依賴)

---

## 功能介紹

### 核心功能

| 功能 | 說明 |
|------|------|
| 📦 需求單管理 | 新增、編輯、刪除需求單，支援重大需求釘選（⭐）與手動排序（▲▼） |
| 📋 子任務管理 | 每筆需求單可建立多個子任務，支援 inline 編輯、拖曳排序、複製 |
| 📊 進度追蹤 | 雙進度條顯示「實際進度」vs「預期進度」，自動標示超前/落後 |
| 🔍 篩選搜尋 | 支援類型、狀態、負責人、成員、關鍵字多維度篩選 |
| 📅 今日儀表板 | 右側側欄自動彙整「逾期」「本週到期」「即將到期」的子任務 |
| 📂 Excel 匯入匯出 | 支援 .xlsx 格式匯入（拖放或點擊），一鍵匯出所有資料 |
| 🤖 AI 子任務建議 | 串接 OpenRouter API，輸入需求名稱自動建議子任務清單 |
| 📊 甘特圖 | 勾選多筆需求單後產出甘特圖，支援匯出為 xlsx |
| 👥 成員管理 | 維護成員清單，支援 Tag 式輸入與自動完成 |
| 📐 範本管理 | 為每種需求類型預設子任務範本，新增時自動套用 |
| 🌙 深色模式 | 一鍵切換亮色/深色主題 |
| 💾 自動儲存 | 所有操作自動存入瀏覽器 localStorage |

---

## 操作說明

### 匯入資料

1. 點擊工具列的 **📂 匯入** 按鈕，或將 `.xlsx` 檔案拖放到看板空白區域
2. 系統自動解析需求單、子任務、成員清單
3. 可選擇「取代現有資料」或「合併匯入」

### 新增需求單

1. 點擊 **＋ 新增** 按鈕
2. 填入需求單編號、名稱、類型、負責人、成員、日期
3. 若該類型有設定子任務範本，儲存後自動產生子任務

### 子任務操作

| 操作 | 方式 |
|------|------|
| 展開卡片 | 點擊需求單標題列 |
| 新增子任務 | 點擊卡片底部「＋ 新增子任務」 |
| Inline 編輯 | 直接在子任務列點擊欄位修改 |
| 拖曳排序 | 按住最左側 **⠿** 把手拖拉 |
| 調整欄寬 | 拖曳欄位標題右側邊緣 |
| 複製 | 點擊 📋 按鈕 |
| 刪除 | 點擊 ✕ 按鈕 |

### 今日儀表板

- 右側側欄自動分類顯示：🔴 逾期 ｜ 🟠 本週到期 ｜ 🟢 即將到期
- 點擊項目可自動捲動到對應的需求卡片
- 側欄支援滑鼠左鍵拖曳捲動

### AI 子任務建議

1. 新增需求單時，點擊名稱旁的 **🤖 AI 拆解** 按鈕
2. 首次使用需輸入 [OpenRouter](https://openrouter.ai/) API Key
3. 選擇模型後點擊「✨ 開始生成」
4. 勾選想要的子任務後點擊「➕ 加入看板」

### 甘特圖

1. 勾選一或多筆需求單（左側核取方塊）
2. 點擊 **📊 甘特圖** 按鈕
3. 可在 Modal 中檢視，或點擊「匯出 xlsx」下載

---

## Excel 匯入匯出格式

### 欄位說明

| 欄位名稱 | 說明 | 範例 |
|----------|------|------|
| 需求單編號 | 唯一識別碼 | `RE2026001` |
| 需求單名稱 | 需求簡述 | `會員登入優化` |
| 需求類型 | 對應範本類型 | `開發功能` |
| 系統別 | 所屬系統 | `投資系統` |
| 負責人 | 多人用「、」分隔 | `宥維、建華` |
| 成員 | 多人用「、」分隔 | `慶龍` |
| 狀態 | 固定四種 | `待開始`/`進行中`/`已完成`/`擱置` |
| 預計開始日 | yyyy-mm-dd | `2026-05-01` |
| 預計完成日 | yyyy-mm-dd | `2026-06-30` |
| 實際完成日 | yyyy-mm-dd | 選填 |
| 備註 | 自由文字 | 選填 |
| 子功能名稱 | 子任務名稱 | `需求規劃` |
| 子功能負責人 | 多人用「、」分隔 | `至軒` |
| 子功能開始日 | yyyy-mm-dd | `2026-05-01` |
| 子功能完成日 | yyyy-mm-dd | `2026-05-10` |
| 子功能實際完成 | yyyy-mm-dd | 選填 |
| 子功能狀態 | 同上四種 | `進行中` |
| 子功能完成率 | 百分比格式 | `60%` |
| 子功能備註 | 自由文字 | 選填 |

> ⚠️ 一筆需求單有多個子任務時，需求單欄位（編號、名稱等）**每行都要重複填寫**，每行代表一個子任務。

### 成員工作表（Sheet 2）

第二個 Sheet 命名為「成員清單」，每列一個成員姓名。

---

## 程式架構

```
PM需求單管理看板_v66.html
│
├── <head>
│   ├── 外部依賴（xlsx.js、exceljs）
│   └── <style> CSS 樣式
│       ├── CSS 變數（亮色/深色主題）
│       ├── 全域元件（header、toolbar、btn、stats、card）
│       ├── 子任務表格（.st-table）
│       ├── 欄寬拖曳（.rz）
│       ├── 子任務拖曳排序（.drag-handle）
│       ├── 側欄（.sidebar）
│       ├── 今日儀表板（.sb-*）
│       └── Modal（.overlay、.modal）
│
├── <body>
│   ├── #appWrapper（主容器，flex-column）
│   │   ├── <header>（頂部標題列）
│   │   ├── #queryZone（查詢操作區，flex-shrink:0）
│   │   │   ├── .toolbar（工具列）
│   │   │   └── #statsBar（統計列）
│   │   └── #mainZone（主內容區，flex:1）
│   │       ├── #boardCol（需求列表，overflow-y:scroll）
│   │       └── #sidebar（今日儀表板，position:absolute right）
│   │
│   └── Modals（需求單、子任務、成員、範本、AI、甘特圖 等）
│
└── <script> JavaScript
    ├── 資料層（localStorage CRUD）
    ├── 假日 & 工作日計算
    ├── 主題切換
    ├── Tag 輸入元件
    ├── 需求類型管理
    ├── Modal 控制
    ├── 需求單 CRUD
    ├── 子任務 CRUD
    ├── 子任務排序/篩選
    ├── 欄寬拖曳調整
    ├── 子任務拖曳排序
    ├── 主渲染函數（render）
    ├── Excel 匯入匯出
    ├── 甘特圖
    ├── AI 子任務建議
    ├── 今日儀表板
    └── 版面高度計算
```

---

## 函數清單

### 資料存取

| 函數 | 說明 |
|------|------|
| `load(key, def)` | 從 localStorage 讀取資料，若不存在則回傳預設值 |
| `save(key, val)` | 將資料序列化後存入 localStorage |
| `saveData()` | 儲存主要資料並顯示儲存提示 |
| `showSaveInd()` | 短暫顯示「已自動儲存」提示文字 |
| `todayStr()` | 回傳今日日期字串（yyyy-mm-dd） |

### 工作日計算

| 函數 | 說明 |
|------|------|
| `fetchHols(year)` | 從 date.nager.at 取得台灣國定假日 |
| `isWorkday(ds)` | 判斷指定日期是否為工作日 |
| `addWorkdays(start, n)` | 從指定日期加上 n 個工作天 |
| `nextWorkday(ds)` | 取得下一個工作日 |
| `genSubtasks(tmpl, startDate)` | 依範本自動產生子任務清單 |

### 需求單操作

| 函數 | 說明 |
|------|------|
| `openReqModal(ri)` | 開啟需求單新增/編輯 Modal |
| `saveReq()` | 儲存需求單表單資料 |
| `deleteReq(ri)` | 刪除指定需求單 |
| `toggleStar(ri)` | 切換重大需求標記 |
| `moveReq(ri, dir)` | 手動調整需求單排序 |
| `toggleCard(ri)` | 展開/收合需求卡片 |

### 子任務操作

| 函數 | 說明 |
|------|------|
| `openStModal(ri, si)` | 開啟子任務編輯 Modal |
| `saveSt()` | 儲存子任務 Modal 資料 |
| `delSt(ri, si)` | 刪除指定子任務 |
| `copySt(ri, si)` | 複製子任務 |
| `addStRow(ri)` | 快速新增空白子任務 |
| `updateStField(ri, si, field, val)` | Inline 更新子任務欄位 |
| `updateStPct(ri, si, val)` | 更新子任務完成率 |
| `initStDragSort(ri)` | 初始化子任務拖曳排序 |
| `startColResize(e, col)` | 啟動欄寬拖曳調整 |

### 渲染

| 函數 | 說明 |
|------|------|
| `render()` | 主渲染函數（統計列 + 需求卡片） |
| `renderSidebar()` | 渲染今日儀表板 |
| `calcProgress(r)` | 計算實際/預期進度 |
| `pctColor(pct)` | 依完成率回傳顏色 |

### Excel 匯入匯出

| 函數 | 說明 |
|------|------|
| `importXlsx(e)` | 處理 xlsx 選擇事件 |
| `processXlsx(file)` | 解析 xlsx 並匯入資料 |
| `exportXlsx()` | 匯出所有資料為 xlsx |

### 甘特圖

| 函數 | 說明 |
|------|------|
| `openGantt()` | 開啟甘特圖 Modal |
| `buildGantt()` | 建立甘特圖 HTML |
| `exportGanttXlsx()` | 匯出甘特圖 xlsx |
| `workdays(s, e)` | 計算工作天數 |

### AI 功能

| 函數 | 說明 |
|------|------|
| `openAiSuggest()` | 開啟 AI 建議 Modal |
| `saveAiKey()` | 儲存 API Key |
| `generateAiSubtasks()` | 呼叫 AI API 產生子任務建議 |
| `insertAiSubtasks()` | 將 AI 建議子任務加入看板 |

---

## 資料結構

### localStorage 鍵值

| Key | 說明 |
|-----|------|
| `pm_v33_data` | 主資料（需求單 + 成員） |
| `pm_v33_types` | 需求類型清單 |
| `pm_v33_tmpl` | 子任務範本 |
| `pm_v33_theme` | 主題設定（light/dark） |

### 需求單物件

```javascript
{
  id: "RE2026001",          // 需求單編號
  name: "功能名稱",          // 名稱
  type: "開發功能",          // 類型
  system: "投資系統",        // 系統別
  owner: "宥維",             // 負責人（「、」分隔）
  devs: "建華、慶龍",        // 成員（「、」分隔）
  status: "進行中",          // 狀態
  startDate: "2026-05-01",  // 預計開始日
  dueDate: "2026-06-30",    // 預計完成日
  actualDate: "",            // 實際完成日
  notes: "備註文字",         // 備註
  starred: false,            // 是否重大需求
  createdDate: "ISO string", // 建立時間
  lastUpdated: "ISO string", // 最後更新時間
  subtasks: [ /* 子任務陣列 */ ]
}
```

### 子任務物件

```javascript
{
  name: "需求規劃",          // 子任務名稱
  person: "至軒",            // 負責人
  startDate: "2026-05-01",  // 開始日
  dueDate: "2026-05-10",    // 預計完成日
  actualDate: "",            // 實際完成日
  status: "待開始",          // 狀態
  pct: 0,                    // 完成率（0~100）
  note: ""                   // 備註
}
```

---

## 技術依賴

| 套件 | 版本 | 用途 |
|------|------|------|
| [SheetJS (xlsx)](https://sheetjs.com/) | 0.18.5 | Excel 讀取 |
| [ExcelJS](https://github.com/exceljs/exceljs) | 4.4.0 | Excel 寫入（含樣式） |
| [date.nager.at API](https://date.nager.at/) | v3 | 台灣國定假日查詢 |
| [OpenRouter API](https://openrouter.ai/) | - | AI 子任務建議（選用） |

> 所有依賴皆透過 CDN 載入，**不需要 npm 或建置流程**，直接用瀏覽器開啟 HTML 即可使用。

---

*最後更新：2026-05-19*
